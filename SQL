CREATE DATABASE palestra;
USE palestra;

CREATE TABLE utenti (
    id INT AUTO_INCREMENT PRIMARY KEY,
    nome VARCHAR(50),
    email VARCHAR(100),
    password VARCHAR(255)
);

CREATE TABLE corsi (
    id INT AUTO_INCREMENT PRIMARY KEY,
    nome VARCHAR(50),
    orario VARCHAR(50),
    posti_disponibili INT
);

CREATE TABLE prenotazioni (
    id INT AUTO_INCREMENT PRIMARY KEY,
    user_id INT,
    corso_id INT,
    FOREIGN KEY (user_id) REFERENCES utenti(id),
    FOREIGN KEY (corso_id) REFERENCES corsi(id)
);

-- corsi di esempio
INSERT INTO corsi (nome, orario, posti_disponibili) VALUES
('Pesi', '10:00', 10),
('Yoga', '15:00', 8),
('Spinning', '18:00', 12);
